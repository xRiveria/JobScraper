{
  "name": "aggregator",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "scraper"
  ],
  "scripts": {
    "scrape": "npm run scrape -w @aggregator/scraper",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w scraper"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  }
}
